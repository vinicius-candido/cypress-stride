{
  "name": "googletest",
  "version": "1.0.0",
  "description": "write a simple web crawler that crawls for the first 10 results of a google search results page.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^10.7.0"
  }
}
